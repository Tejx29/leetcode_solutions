class Solution {
public:
    int thirdMax(vector<int>& nums) {
        int max = 0;
        int thirdmax = 0;

        sort(nums.begin(), nums.end());

        for(int i = 0; i < nums.size(); i++) {

            max = nums[i];

            if(i > 1 && nums[i - 1] == nums[i - 2]) {

                thirdmax = nums[i - 3];

            }

            else if(i > 1 && nums[i - 1] != nums[i - 2]) {

                thirdmax = nums[i - 2];

            }
            
            else {
                
                thirdmax = nums[i];
            
            }

        }

        return thirdmax;

    }
};


-------------------------------------------------------------------

class Solution {
public:
    int thirdMax(vector<int>& nums) {
        int max = INT_MIN;
        int thirdmax = 0;

        sort(nums.begin(), nums.end());

        for(int i = 0; i < nums.size(); i++) {
            
            if(max == nums[i]) {
                
                break;
                
            }

            max = nums[i];
            
            if(i <= 1) {}

            else if(i > 1 && nums[i-1] == nums[i-2]) {

                thirdmax = nums[i - 3];
                
            }

            else {

                thirdmax = nums[i - 2];

            }

            
            if(i > 1 && i == nums.size() - 1) {

                return thirdmax;

            }
        }

        return max;

    }
};


------------------------------------------------

CORRECT!!!

class Solution {
public:
    int thirdMax(vector<int>& nums) {

        set<int> s1;

        for(auto it : nums) {

            s1.insert(it);

        }

        if(s1.size() < 3) {

            auto max = prev(s1.end(), 1);
            return *max;
            
        }

        else {     

            auto thirdmax = prev(s1.end(), 3);
            return *thirdmax;

        }
    }
};

--------------------------------------------------------

auto max_Val = *max_element(nums.begin(), nums.end());
    int tmax = 0;

    int i = 0;

    for(int j = i + 1; j < nums.size(); j++) {

        cout << endl;

        cout << "i: " << i << "j: " << j << endl;
        
        if(nums.size() == 2) {

            cout << "1 if" << endl;

            return max_Val;

        }
        
        if(nums[i] == max_Val) {

            cout << "2 if" << endl;
            
            i++;

        }

        if(nums[j] == max_Val) {

            cout << "3 if" << endl;
            
            continue;

        }

        if(nums[i] < nums[j]) {

            cout << "4 if" << endl;

            tmax = nums[i];

            cout << "tmax 1: " << tmax << endl;

            i++;

        }

        else {

            cout << "else" << endl;

            tmax = nums[j];

            cout << "tmax 2: " << tmax << endl;

            i++;
        }

    }

    return tmax;

    ------------------------------------------------------

    class Solution {
public:
    int thirdMax(vector<int>& nums) {

        int maxi = INT_MIN;
        int tmax = 0;
        
        for(int i = 0; i < nums.size(); i++) {

            if(i == 0 && nums[i] > maxi) {

                cout << "1 if" << endl;

                maxi = nums[i];

                cout << "maxi 1 if: " << maxi << endl;

                continue;
            }    

            if(nums[i] > maxi) {

                cout << "2 if" << endl;

                nums[0] = nums[i];
                nums[1] = maxi;

                maxi = nums[0];

                cout << "nums[0] nums[1] 1 if: " << nums[0] << " " << nums[1] << endl;
                cout << "maxi 2 if: " << maxi << endl;

            }

            else if(nums[i] == maxi) {

                cout << "elif" << endl;

                continue;

            }

            else if(nums[i - 1] - nums[i] == 1) {

                tmax = nums[i];

            } 

            else if(nums[i] < maxi && i == 1 || i == 2) {

                nums[i] = nums[i];

            }

            else {

                cout << "else" << endl;

                nums[2] = nums[i];
                tmax = nums[2];
                cout << "nums[2] nums[i] else: " << nums[2] << " " << nums[i] << endl;

            }

        }

        if(tmax == 0) {
            
            return maxi;

        }

        else {
            
            return tmax;

        }
    }
};

-------------------------------------------------

int first = 0;
    int second = 0;
    int third = 0;

    auto max_val = *max_element(nums.begin(), nums.end());

    for(int i = 0; i < nums.size(); i++) {

        cout << endl;

        cout << "i: " << i << endl;

        if(third == 0 && nums[i] != max_val) {

            third = nums[i];

        }

        else if(third == 0 && nums[i] == max_val) {

            first = nums[i];

        }

        else if(second == 0) {

            second = nums[i];

        }

        else if(first == 0 && nums[i] == max_val) {

            first = nums[i];
        
        }

        else if(nums[i] > first) {

            first = nums[i];

        }

        else if(nums[i] > second) {

            second = nums[i];

        }

        else if(nums[i] > third) {

            third = nums[i];

        }

        else if(nums[i] < third) {

            continue;

        }

        cout << first << " " << second << " " << third << " " << endl;
         
    }

    ----------------------------------------

    #include<iostream>
#include<bits/stdc++.h>

using namespace std;

void thirdMax(vector<int>& nums) {

    auto max_val = *max_element(nums.begin(), nums.end());

    int first = max_val;
    int second = 0;
    int third = 0;

    for(int i = 0; i < nums.size(); i++) {

        cout << endl;

        cout << "i: " << i << endl;

        if(nums[i] == first || nums[i] == second || nums[i] == third) {

            continue;

        }

        if(second == 0) {

            second = nums[i]; 

        }

        else if (third == 0 && nums[i] > second) {

            third = second;
            second = nums[i];

        }

        else if(nums[i] > third) {

            third = nums[i];

        }

        else if(nums[i] < third) {

            continue;

        }

        
        cout << first << " " << second << " " << third << endl;

    }

    cout << "Final: " << first << " " << second << " " << third << endl;

    // if (third == 0) {

    //     return first;

    // }

    // else {
        
    //     return third;

    // }
    
}

int main () {

    int n;

    cout << "Enter size of array: ";
    cin >> n;

    vector<int> arr(n);

    cout << "Enter array elements: " << endl;

    for(int i = 0; i < n; i++) {

        cin >> arr[i];

    }

    thirdMax (arr);
    
    return 0;

}

------------------------------------------------

CORRECTTTTT MINEEEEE

class Solution {
public:
    int thirdMax(vector<int>& nums) {

        auto max_val = *max_element(nums.begin(), nums.end());

        int first = max_val;
        int second = 0;
        int second_exists = INT_MIN;

        int third = 0;
        int third_exists = INT_MIN;

        for(int i = 0; i < nums.size(); i++) {

            cout << endl;

            cout << "i: " << i << endl;

            if((nums[i] == first || nums[i] == second || nums[i] == third) && (nums[i] != 0)) {

                continue;

            }

            if(first == 0 && nums[i] == 0) {

                continue;

            }

            if(second == 0) {

                second = nums[i]; 
                second_exists++;

            }

            else if (third == 0 && nums[i] >= second) {

                third = second;
                second = nums[i];

                third_exists++;
            }

            else if(nums[i] >= second) {

                third = second;
                second = nums[i];

                second_exists++;

            }
            
            else if(nums[i] >= third) {

                third = nums[i];

                third_exists++;

            }

            else if(nums[i] <= third && third != 0) {

                continue;

            }

            
            else if(third == 0) {

                third = nums[i];
                
                third_exists++;

            }
            
            cout << first << " " << second << " " << third << endl;

        }

        // cout << "Final: " << first << " " << second << " " << third << endl;

        if (second_exists == INT_MIN || third_exists == INT_MIN) {

            return first;

        }

        else {
            
            return third;

        }
    }
};