class Solution {
public:
    int thirdMax(vector<int>& nums) {
        int max = 0;
        int thirdmax = 0;

        sort(nums.begin(), nums.end());

        for(int i = 0; i < nums.size(); i++) {

            max = nums[i];

            if(i > 1 && nums[i - 1] == nums[i - 2]) {

                thirdmax = nums[i - 3];

            }

            else if(i > 1 && nums[i - 1] != nums[i - 2]) {

                thirdmax = nums[i - 2];

            }
            
            else {
                
                thirdmax = nums[i];
            
            }

        }

        return thirdmax;

    }
};


-------------------------------------------------------------------

class Solution {
public:
    int thirdMax(vector<int>& nums) {
        int max = INT_MIN;
        int thirdmax = 0;

        sort(nums.begin(), nums.end());

        for(int i = 0; i < nums.size(); i++) {
            
            if(max == nums[i]) {
                
                break;
                
            }

            max = nums[i];
            
            if(i <= 1) {}

            else if(i > 1 && nums[i-1] == nums[i-2]) {

                thirdmax = nums[i - 3];
                
            }

            else {

                thirdmax = nums[i - 2];

            }

            
            if(i > 1 && i == nums.size() - 1) {

                return thirdmax;

            }
        }

        return max;

    }
};


------------------------------------------------

CORRECT!!!

class Solution {
public:
    int thirdMax(vector<int>& nums) {

        set<int> s1;

        for(auto it : nums) {

            s1.insert(it);

        }

        if(s1.size() < 3) {

            auto max = prev(s1.end(), 1);
            return *max;
            
        }

        else {     

            auto thirdmax = prev(s1.end(), 3);
            return *thirdmax;

        }
    }
};

--------------------------------------------------------

auto max_Val = *max_element(nums.begin(), nums.end());
    int tmax = 0;

    int i = 0;

    for(int j = i + 1; j < nums.size(); j++) {

        cout << endl;

        cout << "i: " << i << "j: " << j << endl;
        
        if(nums.size() == 2) {

            cout << "1 if" << endl;

            return max_Val;

        }
        
        if(nums[i] == max_Val) {

            cout << "2 if" << endl;
            
            i++;

        }

        if(nums[j] == max_Val) {

            cout << "3 if" << endl;
            
            continue;

        }

        if(nums[i] < nums[j]) {

            cout << "4 if" << endl;

            tmax = nums[i];

            cout << "tmax 1: " << tmax << endl;

            i++;

        }

        else {

            cout << "else" << endl;

            tmax = nums[j];

            cout << "tmax 2: " << tmax << endl;

            i++;
        }

    }

    return tmax;

    ------------------------------------------------------

    